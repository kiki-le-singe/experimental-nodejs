var bar = 'bar'; // local variable

// `foo` is a global variable because we do not use the `var` keyword.
// We do not need to export it, `foo` is accessible in all modules.
foo = 'foo';

module.exports = {
  bar: bar
}
